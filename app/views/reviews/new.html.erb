<h1>Reviews</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
<%= f.input :content  %>
<%= f.input :rating, collection: [1, 2, 3, 4, 5], label: "Rating (0-5)" %>
<%= f.button :submit, "Submit Review", class: "btn btn-success" %>
<% end %>

<% link_to %>
